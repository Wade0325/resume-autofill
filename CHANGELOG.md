# 版本紀錄

格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循
[語意化版本](https://semver.org/lang/zh-TW/)。0.x 表示還在 beta：功能完整、可以正常使用，
但介面與資料格式仍可能調整。

## [0.1.0] - 2026-09-22

第一個公開版本（beta）。

### 功能

- **我的資料**：十五個主題分頁（基本資料、聯絡方式、學歷、工作經歷、語言能力、求職偏好、
  常見問答、家庭狀況、緊急聯絡人、自傳……），多筆學經歷、證照、語言可自由增減；年齡、總年資、
  服役期間自動計算。大頭照（存檔時轉 JPEG、清掉 EXIF）、版本紀錄（最近 30 份，可還原）、
  匯出備份與從檔案還原
- **填寫履歷**：上傳空白 `.docx`，AI 判斷每一格該填什麼，只改文字、完整保留版面與字型。
  左右對照預覽，填錯的可以換欄位或直接打字改值；「這次應徵」面板填每間公司不同的欄位；
  一次最多 20 份、打包成 zip 下載；列印／存成 PDF（走瀏覽器列印，不必裝轉檔軟體）；
  分析可取消、會排隊、看得到第幾批
- **學過就記住**：同一份格式第二次上傳幾秒完成，模型沒開也能填；學過的格式可以列出、忘掉、重新判讀
- **匯入履歷**：上傳 PDF 或 `.docx`，或直接貼上文字，把資料逐欄抽出來建檔；掃描型 PDF 會改看圖
  判讀並提醒核對；多筆資料會對到已有的那一筆，只補空著的欄位
- **模型管理**：程式內下載 Qwen3.5 系列（可續傳、下載完核對校驗碼、先檢查磁碟空間）、切換、刪除；
  開程式自動載回上次的模型；上下文太小時啟動當下就提醒
- **日誌**：每一步操作的白話紀錄，錯誤訊息附追蹤碼
- **完全在本機執行**：除了第一次下載模型不需要網路；介面、後端、AI 引擎都只綁 localhost；
  個人資料只存在程式資料夾的 `data\`，刪掉整個資料夾即完整移除

### 已知限制

- 只支援 Windows 10／11 64 位元；舊版 `.doc` 要先用 Word 另存成 `.docx`
- 建議 NVIDIA 顯示卡 8 GB VRAM 以上，驅動程式要支援 CUDA 13；沒有獨立顯示卡時用 CPU 推論，
  一份表格要數十分鐘
- 顯示卡記憶體不到 8 GB 時，預設設定（整個模型放上顯示卡）可能載不起來；進階使用者可以用環境變數
  `RESUME_AUTOFILL_GPU_LAYERS` 指定放上顯示卡的層數
- `ResumeAutoFill.exe` 沒有數位簽章，第一次執行時 Windows SmartScreen 會跳出警告，
  按「其他資訊」→「仍要執行」即可
