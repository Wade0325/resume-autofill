# 打包進發佈版的 Python 套件，版本全部鎖死（build-package.ps1 用 --no-deps 照這份裝）。
#
# 這份是 pyproject.toml 的執行期相依，連同所有間接相依，版本取自實際跑過測試與研究迴圈的
# 開發環境——發出去的東西要跟測過的一模一樣，不能讓 pip 在打包當下抓到新版。
# uvicorn 不帶 [standard]：打包版不需要 --reload 與 websockets。
# 升級任何一項都要先在開發環境裝同一版、跑完測試，再改這裡（tests/test_version.py 會檢查
# 這份涵蓋 pyproject 的每一項、而且符合它的版本下限）。
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
fastapi==0.141.1
greenlet==3.5.6
h11==0.16.0
idna==3.19
lxml==6.1.3
pillow==12.3.0
playwright==1.63.0
pydantic==2.13.5
pydantic_core==2.46.5
pyee==13.0.1
pypdfium2==5.13.0
python-docx==1.2.0
python-multipart==0.0.32
requests==2.34.2
starlette==1.6.0
typing_extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
uvicorn==0.52.4
